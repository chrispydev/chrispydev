export interface AboutProps {
  title: string;
  description: string;
  imgUrl: string;
}

export interface WorkProps {
  title: string;
  description: string;
  codeLink: string;
  imgUrl: string;
}
