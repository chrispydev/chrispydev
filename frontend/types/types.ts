export interface AboutProps {
  title: string;
  description: string;
  imgUrl: string;
}

export interface WorkProps {
  title: string;
  name: string;
  description: string;
  codeLink: string;
  imgUrl: string;
  tags: string[];
  projectLink: string;
}
