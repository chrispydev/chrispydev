export interface testimonials {
  name: string;
  company: string;
  feedback: string;
}

export interface Works {
  title: string;
  description: string;
  imgUrl: string;
  body: any;
  link: string;
}
